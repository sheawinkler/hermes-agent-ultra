/target
# 第三方参考仓库通常放在上级目录 ~/workspace/research/GEO-AI
GEO-AI/
**/*.rs.bk
*.pdb
.DS_Store
.env
*.log
vout-endianness-analysis.md
.sync-reports/
.sync-queue/
__pycache__/
**/__pycache__/
*.pyc

# Local quorum/model run captures can include private prompts, trading research,
# and provider error details. Keep them off public main.
artifacts/quorum/

.hermes-agent-ultra
.qoder/
log
# Desktop/bootstrap install marker written into managed checkout roots.
.hermes-bootstrap-complete

# Generated PR infographics live in PR bodies, not in-tree.
infographic/

# Build artifacts
dist/
*.zip

# hermes-talk runtime models (packaging source of truth for `make package-talk-rockchip`)
.models/

# aarch64 cross toolchain cache (copied locally for `cross` Docker; do not symlink outside repo)
.cross-cache/
/.cross-cache/

.local-wip-backup/
.cargo\config.toml

/target-check