#!/usr/bin/env bash
# Auto-format Rust code before commit
cargo fmt --all --check 2>/dev/null
if [ $? -ne 0 ]; then
    echo "Running cargo fmt..."
    cargo fmt --all
    echo "Code formatted. Please re-stage and commit again."
    exit 1
fi
