# Third-Party Notices

Terra Desktop bundles open-source dependencies. See each crate or npm package for its license.

Primary stacks:

- **Frontend**: React, Vite, TanStack Query, Streamdown, Tauri API
- **Desktop shell**: Tauri v2 (MIT / Apache-2.0)
- **Backend**: hermes-http, hermes-tasks, hermes-agent (workspace crates)

Run `npm licenses list` and `cargo license` in CI to regenerate a full SPDX manifest.
