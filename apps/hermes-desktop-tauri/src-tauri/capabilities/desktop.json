{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only: native menu, window controls, clipboard image",
  "windows": ["main"],
  "platforms": ["macOS", "windows", "linux"],
  "permissions": [
    "core:menu:default",
    "core:window:default",
    "core:image:default"
  ]
}
